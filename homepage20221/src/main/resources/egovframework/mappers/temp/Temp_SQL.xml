<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.let.temp.service.impl.TempMapper">
<!-- Ctrl 클릭시  namespace의 위치로 이동 -->

	<resultMap id="temp" type="egovframework.let.temp.service.TempVO">
		<result property="tempId" column="TEMP_ID" />
		<result property="tempVal" column="TEMP_VAL" />
	</resultMap> 
	
	<select id="selectTemp" resultType="tempVO">
		select TEMP_ID as TempId, TEMP_VAL as TempVal from lettemp where TEMP_ID = #{tempId}
	</select>

	<!-- list / limit #{recordCountPerPage} offset #{firstIndex}를 추가하여 한 화면에 10개씩 보여주기
	 #{recordCountPerPage} : class ComDefaultVO에 pageUnit 10개 지정되어 있다 -->
	<select id="selectTempList" resultType="egovMap">
		select TEMP_ID, TEMP_VAL from lettemp limit #{recordCountPerPage} offset #{firstIndex}
	</select>
	
	<!-- 게시글에 대한 총 카운트 수 / 220420 rownum(오라클)과 limit(마리아db) 선언 -->
	<select id="selectTempListCnt" resultType="java.lang.Integer">
		select count(*) cnt from lettemp
	</select>
	
	<!-- insert문 작성 -->
	<insert id="insertTemp" parameterType="tempVO"> <!-- sql_mapper_config.xml에 arias tempVO로 명칭을 정해놔서 parameterType="tempVO"으로 선언 -->
		insert into lettemp (TEMP_VAL) values ( #{tempVal} )
	</insert>
	
	<!-- update문 작성 -->
	<update id="updateTemp" parameterType="egovframework.let.temp.service.TempVO">
		update lettemp set TEMP_VAL = #{tempVal} where TEMP_ID = #{tempId}
	</update>
	
	<!-- delete문 작성 -->
	<delete id="deleteTemp" parameterType="egovframework.let.temp.service.TempVO">
		delete from lettemp where TEMP_ID = #{tempId}
	</delete>

</mapper>